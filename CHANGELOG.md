# Changelog

## Unreleased

- Added SSE verification progress with a JSON fallback.
- Added `/health`, `/metrics`, and `/api/docs` endpoints.
- Added Gunicorn, Docker, Compose, Prometheus scrape configuration, CI, Makefile, and pre-commit configuration.
- Added optional DistilBERT prior-classifier training utility.
- Added deployment documentation and lazy Chroma initialization.
- Added session authentication, user-scoped report history, SSE progress, health, metrics, and OpenAPI discovery endpoints.
